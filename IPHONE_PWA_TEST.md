# 在 iPhone 上安装 Sake Street 测试版

## 安装

1. 将最新版本部署到 Netlify；PWA 必须通过 HTTPS 打开。
2. 在 iPhone 上使用 **Safari** 打开网站地址。
3. 点击 Safari 底部的“分享”按钮。
4. 向下选择“添加到主屏幕”，然后点击“添加”。
5. 回到主屏幕，点击 **Sake Street** 图标启动。

从主屏幕启动后，网站会以独立窗口运行，不显示 Safari 地址栏，并适配 iPhone 刘海和底部安全区。

## 更新测试版

部署新版本后，先在 Safari 中重新打开网站一次，再关闭并重新启动主屏幕上的 Sake Street。网络可用时，应用会更新缓存；已访问过的界面和图片可在短暂断网时继续显示。

## 与 App Store 版本的区别

此版本适合菜单、购物车、下单、员工登录和 Supabase 同步测试，不需要 Mac 或 Apple Developer 账户。它不是 App Store 安装包；相机、推送通知等原生能力仍需后续配置。
